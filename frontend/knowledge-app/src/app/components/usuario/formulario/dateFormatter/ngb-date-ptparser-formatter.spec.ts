import { NgbDatePTParserFormatter } from './ngb-date-ptparser-formatter';

describe('NgbDatePTParserFormatter', () => {
  it('should create an instance', () => {
    expect(new NgbDatePTParserFormatter()).toBeTruthy();
  });
});
