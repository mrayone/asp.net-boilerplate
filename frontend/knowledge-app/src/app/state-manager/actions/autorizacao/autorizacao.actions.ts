import { createAction } from '@ngrx/store';

export const login = createAction('[Autorização Component] Login');
export const atualizarCredencial = createAction('[Autorização Component] Atualizar Credencial');
export const logout = createAction('[Autorização Component] Logout');
